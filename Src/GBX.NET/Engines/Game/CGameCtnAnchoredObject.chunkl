CGameCtnAnchoredObject 0x03101000
- metadata1: x
- metadata2: y

0x002
 version
 ident ItemModel
 vec3 PitchYawRoll
 byte3 BlockUnitCoord
 id AnchorTreeId
 vec3 AbsolutePositionInMap // some comment
 node WaypointSpecialProperty
 if < v5
  int
 if >= v4
  int16 Flags
  if >= v5
   vec3 PivotPosition
   if >= v6
    float Scale
    if >= v7
     if (Flags & 4) = 4
      fileref PackDesc
     if >= v8
      vec3
      vec3
      if >= v9
       throw v

0x004 skippable
 version
 int